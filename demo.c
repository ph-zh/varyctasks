#include <stdio.h>


int main() {

    // arithmetic operators

    // + (addition)
    // - (substraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ (increment)
    // -- (decrement)

    int x = 7;
    int y = 5;

    x++;
    y--;
    printf("%d\n", y);

return 0;
}